<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/new_bookmark_pop"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#2196F3">

    <TextView
        android:id="@+id/new_bookmark_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="10sp"
        android:hint="Remove bookmark confirmation"
        android:text="Are you sure you want to remove this bookmark?"
        android:textSize="@dimen/font_size"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/scripttext"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20sp"
        android:hint="Add annotation"
        android:text=""
        android:textSize="@dimen/font_size"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/new_bookmark_label" />

    <LinearLayout
        android:id="@+id/buttonsLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="20sp"
        android:orientation="horizontal"
        android:gravity="center_horizontal"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent">

        <Button
            android:id="@+id/btnCancel"
            android:layout_width="90sp"
            android:layout_height="wrap_content"
            android:onClick="closePopupWithoutSaving"
            android:text="@string/btn_label_cancel"
            android:textAllCaps="false"
            android:textSize="@dimen/font_size"
            app:layout_constraintStart_toStartOf="parent" />

        <Button
            android:id="@+id/btnYes"
            android:layout_width="90sp"
            android:layout_height="wrap_content"
            android:layout_marginStart="5sp"
            android:onClick="saveBookmarkClosePopup"
            android:text="@string/btn_label_yes"
            android:textAllCaps="false"
            android:textSize="@dimen/font_size"
            app:layout_constraintStart_toStartOf="parent" />

        <Button
            android:id="@+id/btnNo"
            android:layout_width="90sp"
            android:layout_height="wrap_content"
            android:layout_marginStart="5sp"
            android:onClick="saveBookmarkKeepTyping"
            android:text="@string/btn_label_no"
            android:textAllCaps="false"
            android:textSize="@dimen/font_size"
            app:layout_constraintStart_toStartOf="parent" />


    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>